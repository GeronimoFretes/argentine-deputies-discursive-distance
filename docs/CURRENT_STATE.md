> **Superseded:** This operational summary describes an abandoned government–opposition discursive-distance scope. The canonical final summary is [`FINAL_PROJECT_STATE.md`](FINAL_PROJECT_STATE.md).

# Current Project State

Last updated: 2026-06-22

## Purpose

This file is the canonical operational summary of the repository. Read it before beginning work or asking an LLM to modify the project.

For detailed methodology and teammate responsibilities, also read:

* `docs/TEAMMATE_HANDOFF.md`
* `docs/PARALLEL_WORK_PROTOCOL.md`
* `docs/DECISION_LOG.md`

## Research question

How did the discursive distance between governing and opposition blocs evolve in Argentina’s Chamber of Deputies during the recent period with reliable speaker and bloc identification?

The output must be described as semantic or discursive distance. It is not a direct measurement of ideological polarization.

## Current validated checkpoint

The repository currently includes validated implementations for:

* deterministic session discovery
* resumable and column-aware PDF extraction
* structural segmentation
* explicit speaker-marker recognition
* lossless speaker-turn construction
* documentary-insert detection
* exact stage-direction classification
* exact editorial-note classification
* preservation of spoken and unresolved text
* exact source provenance and offsets

Current permanent quality gate:

* 82 tests passing
* Ruff passing
* mypy passing
* six pilot documents passing exact non-speech coverage validation

Validated pilot counts:

| Pilot                   | Stage spans | Editorial spans | Speech words |
| ----------------------- | ----------: | --------------: | -----------: |
| older_ordinary          |          56 |               2 |       73,377 |
| continuation            |          94 |              18 |       41,371 |
| remote_session          |         107 |               5 |       90,481 |
| long_recent_session     |         221 |               8 |      114,763 |
| informative_session     |          46 |               0 |       38,152 |
| latest_ordinary_special |         173 |              33 |       93,379 |

A logical non-speech event may occupy more than one exact source span when it crosses source-block boundaries.

## Active workstreams

### Main pipeline workstream

Branch:

```text
feat/speaker-turn-batch
```

Owner: main developer

Current objective:

* integrate the validated parsers into a resumable corpus-level batch
* persist turns and content spans
* generate deterministic QA summaries
* generate the observed speaker inventory

Primary output needed by the teammate:

```text
session_id
session_date
period
meeting_number
speaker_raw
speaker_normalized
speaker_role
turn_count
speech_span_count
speech_word_count
first_reference
sample_text
```

The main pipeline must not assign legislators, blocs, or political alignment yet.

### Reference-data framework workstream

Branch:

```text
feat/reference-data-framework
```

Owner: teammate

Current objective:

* define political metadata methodology
* define reference CSV schemas
* define manual identity QA
* implement deterministic structural validation
* add validator tests
* prepare report and presentation scaffolds
* prepare the evidence ledger

The first reference-data pull request must contain no factual metadata rows.

All reference CSV files must remain header-only until the observed speaker inventory is available.

## Current population policy

The reference CSVs must not yet contain:

* seed legislators
* example legislators
* current chamber leadership
* example blocs
* example alignments
* synthetic rows
* placeholder factual values

Tests may create temporary fixture files inside the test environment. Those fixtures must not be committed as production metadata.

## Required human review checkpoints

The teammate must stop and request review before:

1. finalizing the `legislator_id` convention;
2. deciding how open-ended intervals are encoded;
3. deciding whether interval endpoints are inclusive;
4. deciding how overlapping memberships are treated;
5. adding any factual metadata rows;
6. assigning any bloc to `government_core` or `opposition_core`;
7. adding a dependency;
8. modifying a shared file;
9. modifying any validated parser module.

## Next integration point

The next cross-workstream integration occurs when the main pipeline produces the observed speaker inventory.

After that:

1. review and approve the stable legislator identifier;
2. populate only observed legislators and required aliases;
3. resolve identities using session dates;
4. populate time-bounded bloc membership;
5. populate time-bounded bloc alignment;
6. implement the deterministic identity-resolution join.

## Result-dependent work

The following must remain placeholders until generated by the analytical pipeline:

* final corpus size
* selected starting year
* identity-resolution rate
* alignment coverage
* final embedding model
* distance time series
* robustness results
* substantive interpretation
* final conclusion
